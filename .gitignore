.anchor
.DS_Store
target
**/*.rs.bk
node_modules
test-ledger
.yarn

# clippy/rustc ICE dumps
rustc-ice-*.txt

# local CLI bundle records
.supersonic

# security-tooling scratch output (regenerable, not part of the deliverable)
.xray/
cargo_audit_full.txt
*.ll

# internal audit-cycle reports (auditor-skill runs) — process artifacts, superseded by AUDIT.md
audit_*/
